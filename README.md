# bottlerocket-bootstrap-instance-id
Bootstrap container for Bottlerocket OS to populate the instance ID to /var/lib/cloud/data/instance-id for Datadog, etc. to consume when access to IMDS is disabled.
